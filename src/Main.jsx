import React from “react”;
import ReactDOM from “react-dom/client”;
import MindPulse from “./MindPulse”;

ReactDOM.createRoot(document.getElementById(“root”)).render(
<React.StrictMode>
<MindPulse />
</React.StrictMode>
);
